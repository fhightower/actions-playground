name: Reusable workflow example

on:
  workflow_call:
    inputs:
      cmd:
        required: true
        type: string

jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v2
      - name: Run cmd
        run: ${{ inputs.cmd }}

